---
title: "TimesURL代码备注"
date: 2026-04-29
draft: false
---

--src，同样的代码，黄先伟的版本



###  数据集 --- ETTm1

​	电力变压器油温数据，由国家电网提供，该数据集是来自中国同一个省的两个不同县的变压器数据，时间跨度为2年，原始数据每分钟记录一次（用 m 标记），每个数据集包含2年 * 365天 * 24小时 * 60分钟 = 1,051,200数据点。由于数据量的庞大，将数据粒度改为每15分钟1个数据点，记为ETTm1、ETTm2，或一个小时级别粒度的数据集变体使用（用 h标记），记为ETTh1、ETTh2。

**数据集包含以下字段：**
	每个数据点均包含8维特征，包括数据点记录日期，预测目标值oil temperature（OT）和6个不同类型功率负载特征。

![image-20250116180901701](/article_images/image-20250116180901701-1737022144564-1.png)



---

load_forecast_csv函数

1. 数据点记录日期$\rightarrow$时间特征（分钟、小时、第几天、等）。
2. 把OT的数据赋给data。
3. 对data做数据划分，分为训练集、测试集、验证集。
4. 为训练集、测试集、验证集生成掩码。1、表示数据有效；0、表示数据为无效（被掩码）；值为 `np.nan` 表示对应位置的数据是缺失的。
5. 对训练集、测试集、验证集进行标准化
6. 